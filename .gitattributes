# ===================================================
# Git Attributes - Gestion fichiers volumineux
# ===================================================

# Fichiers SQL dump (peuvent être très gros)
docker_init/*.sql filter=lfs diff=lfs merge=lfs -text

# Fichiers modèles ML (si présents)
*.pkl filter=lfs diff=lfs merge=lfs -text
*.h5 filter=lfs diff=lfs merge=lfs -text
*.npy filter=lfs diff=lfs merge=lfs -text
*.npz filter=lfs diff=lfs merge=lfs -text

# Fichiers JSON volumineux (corpus)
entrepot_de_donnees/corpus_*.json filter=lfs diff=lfs merge=lfs -text

# DuckDB (si versionné)
*.duckdb filter=lfs diff=lfs merge=lfs -text

# Fin de ligne (normalisation cross-platform)
*.py text eol=lf
*.sh text eol=lf
*.md text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf
*.sql text eol=lf
*.txt text eol=lf
*.env text eol=lf

# Fichiers Windows
*.ps1 text eol=crlf
*.bat text eol=crlf

# Binaires (ne pas modifier)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar.gz binary
*.tgz binary
